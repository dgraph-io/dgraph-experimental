# Prometheus Target Configuration

These files configure Prometheus to scrape metrics from Dgraph Alpha and Zero nodes.

They can be generated using the [`generate_targets.sh`](../generate_targets.sh) script.
